/// writrun:default
# Tasks

**WritRun's default of the same name is in force.** This file is
yours; it simply has nothing of its own to say yet.

To read what is in force — and never by guessing at a path, which
would go stale the day WritRun moved its own:

```bash
bash .writrun/scripts/stage-1-tasks-and-specs/resolve_doc.sh \
  writrun/conventions/tasks.md --origin
```

To answer differently, replace everything in this file — including
the first line — with your own text. What you write then stands
whole: nothing merges, and no update ever touches it again. Leave
it as it is and WritRun's corrections keep reaching you.
