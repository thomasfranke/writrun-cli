# Pipeline

- GoReleaser builds the platform binaries and updates the tap formula
  on each tagged release; the cut itself is
  [release](../versioning/release.md)'s. Release notes name the pinned
  WritRun tag ([architecture](../architecture.md)).
