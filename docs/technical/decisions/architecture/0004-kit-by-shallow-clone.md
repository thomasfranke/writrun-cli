# kit fetched by shallow `git clone` at the pinned tag.

**2026-08-24**

Rejected: GitHub tarball download (adds an HTTP path when git is
already required) and `go:embed` (same second-source-of-truth problem
as embedding scripts).
