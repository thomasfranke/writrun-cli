# the operated repository's scripts are the execution authority.

**2026-08-24**

Rejected: embedding them in the binary — a second source of truth that
drifts from what CI runs and overrides the tag the repository pinned.
