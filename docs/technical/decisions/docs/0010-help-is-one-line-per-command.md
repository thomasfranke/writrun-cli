# `--help` is one line per command plus the docs' address.

**2026-09-03**

Long help text is a second copy of `product/` that drifts. Rejected:
generating help from the docs at build time (machinery to maintain
prose) and full help text (a fork of the product docs).
