# Go, single binary, standard library first.

**2026-08-24**

Rejected: Rust (equivalent fit, higher cost for a thin wrapper), Bash
(no build, but flags/help/distribution become artisanal), interpreted
languages (a runtime requirement the methodology never had).
