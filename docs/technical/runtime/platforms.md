# Platforms

- Supported platforms: macOS, Linux, Windows — cross-compiled without
  cgo.
- On Windows the wrapped scripts still require `bash` (Git Bash or
  WSL); [`doctor`](../../product/adoption/doctor.md) reports its
  absence.
